NAVBAR: goto() NAVIGATION - Test Matrix
=======================================

| Test Case | Action | Expected Result |
|-----------|--------|----------------|
| Desktop Nav Home | Click "KissOO Svelte 5" | goto('/') - no page reload |
| Desktop Nav Houses | Click "Houses" | goto('/houses') - no reload |
| Desktop Nav Owners | Click "Owners" | goto('/owners') - no reload |
| Desktop Nav Cleaners | Click "Cleaners" | goto('/cleaners') - no reload |
| Desktop Nav Bookings | Click "Bookings" | goto('/bookings') - no reload |
| Desktop Nav Schedules | Click "Schedules" | goto('/schedules') - no reload |
| Desktop Nav Users | Click "Users" | goto('/users') - no reload |
| Desktop Nav Login | Click "Login" | goto('/login') - no reload |
| Desktop Nav Signup | Click "Signup" | goto('/signup') - no reload |
| Mobile Nav Open | Click hamburger | Menu opens |
| Mobile Nav Click | Click any link | goto(path) + menu close |
| Back Button | Navigate, then back | History works correctly |
| Forward Button | Navigate back, then forward | History works correctly |

VERIFICATION STEPS:
1. Open DevTools → Network tab
2. Disable cache (optional)
3. Click each navbar link
4. VERIFY: No "(index)" or "document" entries in Network tab
5. VERIFY: URL changes but page does NOT reload
6. VERIFY: Mobile menu closes after nav
7. VERIFY: Browser back/forward work

